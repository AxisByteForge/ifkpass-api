# AWS Configuration
STAGE=dev
REGION=us-east-1
ACCOUNT_ID=972210179301

# Cognito Configuration
COGNITO_URL=https://cognito-idp.us-east-1.amazonaws.com
COGNITO_CLIENT_ID=your_cognito_client_id
COGNITO_CLIENT_SECRET=your_cognito_client_secret
COGNITO_USER_POOL_ID=your_user_pool_id

# Email Service (Resend)
RESEND_MAIL_API_KEY=your_resend_api_key

# Security
BCRYPT_SALT_ROUNDS=10

# Database and Storage (configured per environment in CDK stack)
# USERS_TABLE_NAME is set automatically based on STAGE
# PROFILE_BUCKET_NAME is set automatically based on STAGE

